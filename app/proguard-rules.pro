# Room, Glance and the ad SDK ship their own consumer rules; this file is for
# anything this app needs on top. Verify a release build before adding blanket
# -keep rules — they defeat the shrinking the release build type exists for.
